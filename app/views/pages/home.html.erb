
<!-- ARTICLES -->
<div class="full-section d-block p-4">
  <div class="container">
    <div class="section-landing">
      <h2 class="category-heading"><%= Category.first.name %></h2>
    </div>

    <div class="stickyscroll" style="margin-bottom: 120px;">
      <div class="col" id="stickymove">
        <% @articles.where("category_id = ?", Category.first.id).each do |article| %>
        <%= link_to category_article_path(article.category_id, article.id), class:"hover-cards" do %>
        <div class="sticky-element align-items-center justify-content-center" style="background-image: url(<%= article.img_url %>); background: linear-gradient(rgba(0,0,0,0.1), rgba(0,0,0,0.2)) , url(<%= article.img_url %>) center center;">
          <h3 class="image-title-white"><%= article.title %></h3>
        </div>
        <% end  %>
        <% end  %>
      </div>
      <div class="col">
        <% @articles.where("category_id = ?", Category.first.id).sample(2).each do |article| %>
        <%= link_to category_article_path(article.category_id, article.id) do %>
        <div>
          <div class="card-bg" style="background-image: url(<%= article.img_url %>);"></div>
          <div class="p-2 mb-3">
            <p class="card-author-up">#<%= Category.find(article.category_id).name %></p>
            <h4 class="mb-4"><%= article.title %></h4>
            <p class="card-author-up author-border"><%= article.source %></p>
          </div>
        </div>
        <% end %>
        <% end %>
      </div>

      <div class="col">
        <% @articles.where("category_id = ?", Category.first.id).sample(2).each do |article| %>
        <%= link_to category_article_path(article.category_id, article.id) do %>
        <div>
          <div class="card-bg" style="background-image: url(<%= article.img_url %>);"></div>
          <div class="p-2 mb-3">
            <p class="card-author-up">#<%= Category.find(article.category_id).name %></p>
            <h4 class="mb-4"><%= article.title %></h4>
            <p class="card-author-up author-border"><%= article.source %></p>
          </div>
        </div>
        <% end %>
        <% end %>
      </div>
    </div>


    <!--         EVENTS -->
    <div class="section-landing mt-4">
      <h2 class="category-heading">Experiences</h2>
    </div>
    <div>
      <div class="d-flex justify-content-between event-section">
        <% @events.sample(5).each do |event|%>
        <%= link_to event do %>
        <div class="event-card">
          <div class="card-bg" style="background-image: url(<%= event.photo %>);"></div>
          <div class="p-2">
            <p class="card-author-up">#<%= Category.find(event.category_id).name %></p>
            <h4 class="mb-4"><%= event.title %></h4>
            <p class="card-author-up author-border"><%= event.address %></p>
          </div>
        </div>
        <% end %>
        <% end %>
      </div>
      <h5><%= link_to "Show all experiences >",events_path, style:"text-decoration:none"%></h5>
    </div>
  </div>
</div>
<div class="do-the-thing">
  <div class="elevator">
    <%= link_to "#", id: "scroll-to-top-button" do %>
      <svg class="sweet-svg" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" x="0px" y="0px" viewBox="0 0 100 100" enable-background="new 0 0 100 100" xml:space="preserve" height="100px" width="100px">
        <path d="M70,47.5H30c-1.4,0-2.5,1.1-2.5,2.5v40c0,1.4,1.1,2.5,2.5,2.5h40c1.4,0,2.5-1.1,2.5-2.5V50C72.5,48.6,71.4,47.5,70,47.5z   M47.5,87.5h-5v-25h5V87.5z M57.5,87.5h-5v-25h5V87.5z M67.5,87.5h-5V60c0-1.4-1.1-2.5-2.5-2.5H40c-1.4,0-2.5,1.1-2.5,2.5v27.5h-5  v-35h35V87.5z"/>
        <path d="M50,42.5c1.4,0,2.5-1.1,2.5-2.5V16l5.7,5.7c0.5,0.5,1.1,0.7,1.8,0.7s1.3-0.2,1.8-0.7c1-1,1-2.6,0-3.5l-10-10  c-1-1-2.6-1-3.5,0l-10,10c-1,1-1,2.6,0,3.5c1,1,2.6,1,3.5,0l5.7-5.7v24C47.5,41.4,48.6,42.5,50,42.5z"/>
      </svg>
      Back to Top
    <% end %>
  </div>
</div>
