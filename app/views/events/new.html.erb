<div>
  <div class="row">
    <div class="col-6" style="height:100vh">
      <div>
        <div id="form-event" class="d-flex justify-content-center align-items-center flex-column" style="height:100vh;">
          <h2 class="devise-heading">List Your Event</h2>
          <div class="form-group">
            <%= simple_form_for [@event] do |f| %>
            <%= f.input   :title,
            placeholder: "Event title",
            label: false,
            required: true %>
            <%= f.input   :address,
            placeholder: "Address",
            label: false,
            required: true %>

            <%= f.input   :price,
            as: :currency,
            placeholder: "Price",
            label: false,
            required: true %>
            <%= f.input   :photo,
            placeholder: "Photo URL",
            label: false,
            required: true %>
            <%= f.input :date,
            as: :string,
            required: false,
            input_html: {class: "datepicker"} %>
            <%= f.input  :description %>
            <%= f.input   :category_id,
            placeholder: "Event category",
            :collection => @categories,
            label: false,
            required: true %>

            <div class="form-actions btn-login">
              <%= f.button :submit, "Create an Event",  class: "btn btn-red" %>
            </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
    <div class="col-6">
      <div class="new-event-bg"></div>
    </div>
  </div>
</div>

